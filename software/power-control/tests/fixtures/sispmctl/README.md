# Placeholder fixtures

These files document the currently observed output shape from the prototype and are suitable for
parser development only. Before enabling the real adapter, replace or confirm them with complete
stdout, stderr, exit-code, timing and environment captures from the Raspberry Pi running
`sispmctl 4.11`.
