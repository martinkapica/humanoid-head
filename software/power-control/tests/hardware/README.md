# Hardware tests

Real-controller tests belong here after the fixture set and hardware acceptance checklist are
complete. They must be opt-in, require exactly one controller, and never run in the default
`pytest` suite.
